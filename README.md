#Comandos para iniciar FASTAPI> 
python -m pip install fastapi[standard] sqlmodel  (requirements.txt)
python -m fastapi dev main.py  
Para iniciar la IA de moderacion de imagenes>
docker compose up --build     